# Dependencies & build artifacts
node_modules
.next
out
dist
coverage

# Git / IDE
.git
.gitignore
.cursor
.vscode
.idea

# Env & local data (runtime uses compose volumes + env_file)
.env
.env.*
!.env.example
.data

# Docs / plans (not needed in image)
docs
*.md
!README.md

# Docker itself (avoid recursive context noise)
Dockerfile*
docker-compose*.yml
.dockerignore

# Misc
npm-debug.log*
.DS_Store
agent-transcripts
